{
  "name": "loud",
  "version": "0.7.3",
  "description": "Web accessibility testing helper",
  "keywords": [
    "accessibility",
    "a11y",
    "test",
    "testing",
    "unit-tests"
  ],
  "author": "Ruslan Sagitov <rs@ruslansagitov.name>",
  "license": "MIT",
  "homepage": "https://github.com/ruslansagitov/loud",
  "repository": {
    "type": "git",
    "url": "https://github.com/ruslansagitov/loud.git"
  },
  "bugs": {
    "url": "https://github.com/ruslansagitov/loud/issues",
    "email": "rs@ruslansagitov.name"
  },
  "main": "lib/loud.js",
  "directories": {
    "lib": "./lib"
  },
  "scripts": {
    "test": "npm run-script lint && npm run-script node-test && npm run-script karma-test",
    "node-test": "mocha --reporter=dot ./test/test.js",
    "karma-test": "karma start --single-run",
    "lint": "eslint lib/ test/ && jscs lib/ test/",
    "coverage": "istanbul cover _mocha -- --reporter=dot ./test/test.js",
    "jsdoc": "jsdoc -d jsdoc lib/loud.js README.md",
    "build": "npm run-script browserify && npm run-script browserify-tests",
    "browserify": "browserify --standalone loud -o ./dist/loud.js ./lib/loud.js",
    "browserify-tests": "browserify -x jsdom -o ./build/test.js ./test/test.js"
  },
  "dependencies": {
  },
  "devDependencies": {
    "benchmark": "^1.0.0",
    "browserify": "^10.2.1",
    "es5-shim": "^4.1.3",
    "html5shiv": "^3.7.2",
    "istanbul": "^0.3.14",
    "jsdom": "^3.1.2",
    "karma": "^0.12.32",
    "karma-mocha": "^0.1.10",
    "mocha": "^2.2.5"
  },
  "engines": {
    "node": ">= 0.10.0"
  }
}
